<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.TP5" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.TP5" parent="Base.Theme.TP5" />

    <!-- Corrected text style -->
    <style name="text">
        <item name="android:textSize">17sp</item> <!-- 'sp' should be used for text size instead of 'dp' -->
        <item name="android:paddingBottom">10dp</item>
        <item name="android:textColor">@android:color/holo_green_dark</item>
    </style>

    <!-- Corrected input style -->
    <style name="input">
        <item name="android:paddingBottom">20dp</item>
    </style>
    <style name="InputStyle">
        <item name="android:background">@drawable/input_background</item>
        <item name="android:padding">12dp</item>
        <item name="android:layout_marginBottom">16dp</item>
    </style>

</resources>
